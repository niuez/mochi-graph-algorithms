version: 2

jobs:
  build:
    docker:
      - image: circleci/rust:1.30.1
    
    steps:
      - checkout
      - run:
          name: test3
          command: |
            cargo test test3 -- --nocapture

